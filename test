import requests

def test_web_app_service():
    url = "energywisewebapp.azurewebsites.net"
    response = requests.get(url)

    if response.status_code == 200:
        print("Web app service is healthy")
    else:
        print("Web app service is not healthy")

test_web_app_service()
